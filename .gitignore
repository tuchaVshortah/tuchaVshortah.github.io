/public/
# Node
node_modules/
portfolio/node_modules
.next/
out/
dist/

# Résumé PDF — fetched from the resume repo's latest release by CI at build
# time (see .github/workflows/ci.yaml). Never commit it, or the tracked copy
# will drift from the actual résumé.
portfolio/public/resume.pdf

# Hugo
.hugo_build.lock
blog/resources/_gen/

# OS
.DS_Store

# Env
.env
.env.local

# Logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
